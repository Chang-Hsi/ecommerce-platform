import { PlaceholderPage } from "@/features/common/PlaceholderPage";

export default function LoginRoutePage() {
  return (
    <PlaceholderPage
      title="登入"
      description="登入流程將在後續里程碑接入 JWT / OAuth 與權限控管。"
    />
  );
}
